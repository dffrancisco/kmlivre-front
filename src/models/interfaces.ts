export interface iLogin {
    name: string;
    id_user: string;
    email: string;
    phone: string;
    token: string;
}

export interface iUsuario {
    access: string
    date_in: string
    email: string
    id_user: number
    name: string
    phone: string
}